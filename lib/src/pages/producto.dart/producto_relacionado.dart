import 'package:flutter/material.dart';

class ProductoRelacionado extends StatelessWidget {
  const ProductoRelacionado({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: _body(),
    );
  }

  _body() {
    return SizedBox();
  }
}
